database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/petdemo}
spring.datasource.username=${POSTGRES_USER:petdemo}
spring.datasource.password=${POSTGRES_PASS:petdemo}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
